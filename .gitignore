node_modules/
dist/
src-tauri/target/
src-tauri/gen/

# Working notes: plan of record and running progress log. Local only.
.plans/

# Editor tasks/settings. Local only.
.vscode/

# Maintainer-only release helpers — need repo write access and the signing key.
# The committed reference is docs/RELEASING.md.
scripts/

# Updater signing keys must never be committed
*.key
*.key.pub
.env
.env.local

.DS_Store
Thumbs.db
*.log
